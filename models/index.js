const User = require('./User');
const Appointment = require('./Appointment');  // Note: singular
const Professional = require('./Professional');

// Change this line to use the correct variable name (Appointment, not Appointments)
module.exports = { User, Appointment, Professional };
